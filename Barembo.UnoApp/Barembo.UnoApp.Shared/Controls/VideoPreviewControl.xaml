<UserControl
    x:Class="Barembo.UnoApp.Shared.Controls.VideoPreviewControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Barembo.UnoApp.Shared.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Border CornerRadius="5">
        <Grid>
            <Image Source="{x:Bind Image1}" Visibility="{x:Bind ShowImage1,Mode=OneWay}" Stretch="UniformToFill"/>
            <Image Source="{x:Bind Image2}" Visibility="{x:Bind ShowImage2,Mode=OneWay}" Stretch="UniformToFill"/>
            <Image Source="{x:Bind Image3}" Visibility="{x:Bind ShowImage3,Mode=OneWay}" Stretch="UniformToFill"/>
            <Image Source="{x:Bind Image4}" Visibility="{x:Bind ShowImage4,Mode=OneWay}" Stretch="UniformToFill"/>
            <Image Source="{x:Bind Image5}" Visibility="{x:Bind ShowImage5,Mode=OneWay}" Stretch="UniformToFill"/>
            <Image Source="{x:Bind Image6}" Visibility="{x:Bind ShowImage6,Mode=OneWay}" Stretch="UniformToFill"/>
            <Border Background="White" Width="40" Height="40" CornerRadius="20" Opacity="0.6">
                <SymbolIcon Symbol="Play" Foreground="Black" />
            </Border>
        </Grid>
    </Border>
</UserControl>
