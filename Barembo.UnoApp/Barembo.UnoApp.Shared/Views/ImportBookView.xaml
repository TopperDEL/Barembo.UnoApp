<Page
    x:Class="Barembo.UnoApp.Shared.Views.ImportBookView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Barembo.UnoApp.Shared.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:prismMvvm="using:Prism.Mvvm"
    prismMvvm:ViewModelLocator.AutowireViewModel="True"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel>
            <Button x:Uid="ScanQRCodeButton" Content="Scan QR-code" Command="{Binding ScanQRCodeCommand}"/>
            <TextBox Text="{Binding MagicLink, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Button x:Uid="ImportMagicLinkButton" Content="Import Magic-Link" Command="{Binding ImportMagicLinkCommand}"/>
            <Button x:Uid="GoBackButton" Content="Go back" Command="{Binding GoBackCommand}"/>
        </StackPanel>
    </Grid>
</Page>
